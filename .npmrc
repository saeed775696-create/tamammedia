# next-auth 4 declares Nodemailer 7 as an optional peer for its Email provider.
# This application uses Credentials auth and its own patched Nodemailer 9 SMTP
# transport, so retaining the insecure peer range is neither necessary nor safe.
legacy-peer-deps=true
