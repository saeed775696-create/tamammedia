# دليل التسليم والنشر للإنتاج

هذا الدليل يجهّز موقع تمام ميديا للنشر على Vercel وربطه بالدومين الرسمي، مع تشغيل Google Analytics وSearch Console بصورة آمنة. لا تضع أي كلمة مرور أو ملف JSON أو مفتاح خدمة في Git أو في تذكرة دعم أو رسالة محادثة.

## 1. قرار النطاق

استخدم الدومين الرسمي بصيغة `https://example.com` كمصدر وحيد للحقيقة. أضف النسخ الأخرى (`www.example.com` و`example.com`) إلى Vercel، ثم عيّن واحدة فقط كنطاق أساسي واجعل الأخرى تعيد التوجيه 308 إليه.

بعد معرفة الدومين الرسمي، أضف في Vercel > Project > Settings > Environment Variables، لقيمة **Production** على الأقل:

| المتغير | القيمة |
| --- | --- |
| `NEXTAUTH_URL` | `https://example.com` |
| `NEXT_PUBLIC_APP_URL` | `https://example.com` |
| `ADMIN_HOST` | اسم المضيف العشوائي للوحة فقط، مثل `m7x4p9ka3qv6.example.com` |
| `NEXTAUTH_SECRET` | قيمة جديدة بطول 32 بايت أو أكثر |
| `ANALYTICS_ENCRYPTION_KEY` | قيمة مستقلة جديدة بطول 32 بايت أو أكثر |
| `DATABASE_URL` | رابط Supabase Pooler بمنفذ `6543` و`pgbouncer=true` |
| `DIRECT_URL` | رابط Supabase المباشر بمنفذ `5432` |
| `NEXT_PUBLIC_SUPABASE_URL` | رابط مشروع Supabase |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | مفتاح Supabase العام فقط |
| `SUPABASE_SERVICE_ROLE_KEY` | مفتاح Supabase الخدمي السري |
| `SMTP_HOST`، `SMTP_PORT`، `SMTP_SECURE`، `SMTP_USER`، `SMTP_PASS`، `SMTP_FROM` | بيانات صندوق البريد المخصص للاسترجاع |

مثال لتوليد المفتاحين محليًا (كل أمر يولّد قيمة مختلفة):

```powershell
openssl rand -base64 32
```

لا تغيّر `NEXTAUTH_SECRET` أو `ANALYTICS_ENCRYPTION_KEY` بعد دخول الإنتاج دون خطة تدوير: الأول ينهي جلسات المستخدمين، والثاني يمنع قراءة اتصال Google Analytics المشفّر إلى أن يُدخل ملف JSON من جديد.

عند تفعيل لوحة التحكم على نطاق فرعي، تصبح القيمتان مختلفتين عمدًا: اجعل `NEXT_PUBLIC_APP_URL` للدومين العام، و`NEXTAUTH_URL` للنطاق الفرعي الإداري. لا تضع قيمة `ADMIN_HOST` في متغير يبدأ بـ`NEXT_PUBLIC_`.

## 2. ربط الدومين في Vercel

1. افتح مشروع `tamammedia-website` في Vercel، ثم **Settings > Domains**.
2. أضف `example.com` و`www.example.com`.
3. انسخ سجلات DNS التي يعرضها Vercel بالضبط إلى مزوّد الدومين. عادةً يكون سجل `A` للجذر وسجل `CNAME` لـ `www`، لكن اعتماد السجلات المعروضة في Vercel هو المرجع النهائي.
4. انتظر حتى تظهر حالة **Valid Configuration** وشهادة SSL فعّالة.
5. اجعل نطاقًا واحدًا فقط هو **Primary**، وفعّل إعادة توجيه الآخر إليه.
6. غيّر القيمتين `NEXTAUTH_URL` و`NEXT_PUBLIC_APP_URL` إلى النطاق الأساسي، ثم أعد النشر.

يبقى نطاق Vercel المؤقت للاختبار فقط؛ التطبيق يرسل له `noindex` كي لا ينافس الدومين الرسمي في البحث.

### عزل لوحة التحكم على نطاق فرعي عشوائي

1. ولّد اسمًا عشوائيًا من 12 إلى 16 حرفًا وأرقامًا؛ لا تستخدم `admin` أو `dashboard` أو اسم الشركة. مثال توضيحي فقط: `m7x4p9ka3qv6.example.com`.
2. أضف الاسم إلى **Vercel > Settings > Domains** في **المشروع نفسه**، ثم أضف سجل DNS الذي تعرضه Vercel.
3. في متغيرات **Production** اضبط:

   ```env
   NEXT_PUBLIC_APP_URL="https://example.com"
   NEXTAUTH_URL="https://m7x4p9ka3qv6.example.com"
   ADMIN_HOST="m7x4p9ka3qv6.example.com"
   ```

4. أعد النشر بعد ظهور حالة **Valid Configuration** وشهادة SSL فعّالة.
5. تحقّق أن `https://example.com/dashboard` و`/login` يعيدان 404، وأن النطاق الفرعي يفتح لوحة التحكم فقط ويرسل `/` إلى `/dashboard`.

لا تعتمد على الاسم العشوائي كبديل للمصادقة: سجلات DNS وشهادات TLS قد تكشف النطاق. يظل عزل الكوكيز، الأدوار، حدود المحاولات، وCSP هي الحماية الأساسية.

يتحقق التطبيق في الإنتاج من تطابق اسم المضيف في `NEXTAUTH_URL` و`ADMIN_HOST`. هذا مقصود: إذا أخطأت في أحدهما، يفشل البناء بدل تشغيل جلسات الإدارة بإعداد غير آمن.

يعتمد العزل على رأس `Host` الذي يتحقق منه Vercel قبل تمريره للتطبيق. لا تشغّل هذه النسخة خلف وكيل يعيد تمرير رأس `Host` من العميل بلا تحقق.

## 3. Google Analytics 4 ولوحة الإحصاءات

يتكوّن الربط من جزأين مختلفين:

- **Measurement ID** يبدأ بـ `G-`: يشغّل تتبع الزيارات في الموقع العام.
- **Property ID** رقمي فقط: يقرأ الإحصاءات داخل لوحة التحكم.

خطوات الربط:

1. أنشئ أو افتح خاصية GA4، وأضف Web Data Stream للدومين الرسمي.
2. من إعدادات الـ Stream انسخ الـ **Measurement ID**.
3. في Google Cloud أنشئ حساب خدمة منفصلًا خاصًا بالموقع، وفَعِّل **Google Analytics Data API** في المشروع نفسه.
4. في Google Analytics: **Admin > Property access management**، أضف بريد حساب الخدمة بدور **Viewer**. منحه دور IAM في Google Cloud وحده غير كافٍ.
5. أنشئ مفتاح JSON واحدًا للحساب الخدمي واحفظه في مدير أسرار آمن.
6. سجّل دخول المدير إلى `/dashboard` ثم **إعدادات الموقع > التحليلات وGoogle**، والصق Measurement ID وProperty ID وملف JSON، ثم اضغط **ربط Google والتحقق**.
7. افتح `/dashboard/analytics` وتحقق من ظهور حالة الاتصال ومن التقرير بعد وصول بيانات GA4.

ملف JSON لا يُرسل للمتصفح بعد الحفظ: يخزّنه التطبيق مشفّرًا في قاعدة البيانات باستخدام `ANALYTICS_ENCRYPTION_KEY`. لا تضعه في متغير بيئة أو ملف مستودع ما دام الربط يتم من لوحة التحكم.

### تدوير مفتاح Google السابق

أي مفتاح حساب خدمة تمت مشاركته خارج مدير الأسرار يجب اعتباره مكشوفًا. قبل التسليم النهائي:

1. أنشئ مفتاحًا جديدًا للحساب الخدمي أو حساب خدمة جديدًا مخصصًا للموقع.
2. حدّث الاتصال من لوحة التحكم بالمفتاح الجديد وتأكد أن الاختبار نجح.
3. احذف المفتاح القديم من Google Cloud > IAM & Admin > Service Accounts > Keys.
4. راجع أن المستودع لا يحتوي `*.json` خاصة بحسابات الخدمة (`git grep -n "BEGIN PRIVATE KEY"`).

## 4. Google Search Console وSEO

1. أضف خاصية **Domain property** للدومين الرسمي في Google Search Console وأكّد الملكية بسجل DNS TXT.
2. بعد تأكيدها، أرسل `https://example.com/sitemap.xml`.
3. من لوحة التحكم: **إعدادات الموقع > محركات البحث SEO**، أدخل قيمة `content` فقط من وسم `google-site-verification` إذا اخترت تحقق الوسم بدل DNS.
4. افحص عنوانًا عامًا عبر URL Inspection بعد النشر. لا ترسل `/dashboard` أو `/login` للفهرسة.

## 5. البريد واسترجاع كلمة المرور

أنشئ صندوقًا مخصصًا مثل `no-reply@example.com` أو استخدم مزود SMTP موثوقًا. لا تستخدم كلمة مرور البريد الأساسية؛ استخدم كلمة مرور تطبيق أو بيانات SMTP مخصصة. اختبر من الإنتاج:

1. طلب «نسيت كلمة المرور».
2. وصول رمز واحد فقط إلى البريد الصحيح ضمن الزمن المتوقع.
3. تعذر إعادة استخدام الرمز بعد نجاح التغيير أو بعد انتهاء صلاحيته.
4. تغيير البريد وكلمة المرور من لوحة المدير.

## 6. فحص القبول قبل التسليم

- تظهر الصفحة العامة و`/sitemap.xml` على الدومين الرسمي عبر HTTPS.
- جميع canonical وOpen Graph تشير إلى الدومين الرسمي، لا إلى نطاق Vercel.
- تسجيل الدخول ولوحة التحكم لا يُخزّنان في CDN (`Cache-Control: no-store`) وهما خارج الفهرسة.
- الصفحات العامة تستفيد من التخزين المؤقت ISR/CDN؛ لا تظهر جلسة أو بيانات إدارة في HTML العام.
- رفع صورة وتعديل/إضافة/حذف خدمة أو مشروع من اللوحة ينعكس في الموقع وفي sitemap.
- الاتصال بـ Google Analytics ناجح، وMeasurement ID ظاهر في إعدادات الموقع دون عرض بيانات حساب الخدمة.
- لا توجد مفاتيح أو كلمات مرور في Git أو في لقطات الشاشة أو رسائل البريد.
- نفّذ `npm run lint` و`npm run typecheck` و`npm run build` قبل اعتماد آخر نشر.

## 7. النشر والاسترجاع

المستودع الذي يتصل به Vercel هو `haithamalsh/tamammedia-website` وفرع النشر `main`. بعد دمج التغييرات على `main` يبدأ Vercel نشرًا تلقائيًا.

إذا وقع خلل بعد الإطلاق، لا تعدّل قاعدة البيانات عشوائيًا: من Vercel > Deployments اختر آخر نشر ناجح واضغط **Promote to Production**. راجع السجلات أولًا، ثم أصلح السبب في فرع Git وانشر إصلاحًا جديدًا.

## بيانات التسليم التي يجب حفظها خارج المستودع

- حساب مالك Vercel وحساب مالك GitHub.
- حساب مالك Supabase ونسخة احتياطية مجرّبة لقاعدة البيانات.
- حساب مالك الدومين وDNS.
- حساب مالك Google Analytics وGoogle Search Console وGoogle Cloud.
- مدير أسرار يحتوي مفاتيح SMTP وSupabase وحساب Google الخدمي ومفاتيح NextAuth والتشفير.
